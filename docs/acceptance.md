# 本次交付与验收记录

记录日期：2026-09-20（Australia/Perth）。工作分支：`codex/complete-dbops-v1`，基线：`30e53c4`。这是一份可复核的开发验收记录，不是生产发布声明。

## 已完成并验证

| 范围 | 验证方式与结果 |
|---|---|
| Go 后端 | `go test -race ./...`、`go vet ./...` 通过 |
| Web | `npm ci`、TypeScript 检查和 Vite 生产构建通过；存在主包体积提示，不影响构建 |
| MySQL 真实安装 | 隔离 MySQL 8.0.46 Linux/AMD64 容器内，真实 mysqld 初始化与 process 模式启动，root 认证、拒绝无密码访问、bootstrap SQL 清理通过；复用官方镜像客户端/服务端，通过测试包装 tar 进入安装器 |
| MySQL 真实逻辑恢复 | 真实 mysqldump、SHA256、gzip、拒绝非空目标、拒绝错误摘要，恢复两条测试记录并核对内容通过 |
| MySQL 双机 systemd/GTID | clp01 与 clp02（Ubuntu 24.04 ARM64）使用独立目录和服务名完成 MySQL 8.0.46 安装；手工一致基线后创建 GTID 复制，IO/SQL 线程 Yes、延迟 0；主库新增记录在从库可见；从库 systemd 重启后复制仍为健康 |
| 恢复范围限制 | 拒绝 all_databases、MySQL 系统 schema 和未知备份范围；只允许显式用户库进入运行时检查 |
| 任务可靠性 | 并发领取唯一性、所有权完成、续租、停机中断、过期恢复，以及周期备份提交后宕机的幂等测试通过 |
| 平台快照 | WAL 数据保存、完整性与摘要校验、恢复到新目录、拒绝已有目标、拒绝篡改通过 |
| 权限 | 项目/环境联合隔离、列表过滤、越权拒绝、无 Content-Type 与大小写字段绕过拒绝、撤权即时生效通过 |
| Agent TLS | 配置错误拒绝启动，无客户端证书及证书 CN/Agent UUID 不匹配拒绝通过 |
| 通知 | 本机 HTTP 测试接收端验证失败重试、静默、恢复事件去重；未连接真实邮箱或消息接收方 |
| 现有 API 控制链路 | 以当前 Linux 二进制在隔离容器执行 ci、auth-ci、mysql-replication-ci、oracle-ci、postgres-ci、doris-ci 六套工作流脚本，全部通过；数据库命令为测试替身 |
| Web 浏览器验收 | 登录、空资产 Dashboard、必填校验、提交平台快照并在任务详情看到成功、备份计划创建/停用、监控空态通过 |
| 容器部署 | 多阶段 Docker 构建通过；非 root Server 在新命名卷上启动、登录、内嵌 Web、平台快照通过；删除并重建容器后任务和快照仍在。测试容器/卷已清理 |

自动化入口：`.github/workflows/hardening-ci.yml` 和 `scripts/test-real-mysql.sh`。开发环境的详细日志存于忽略目录 `.local-test/`，不把临时数据库、测试密钥、二进制或日志提交到仓库。

## 实现边界和仍需完成的设计项

以下项目尚不能标记为“完整 V1.0 已完成”：

- MySQL Linux systemd 的真实主机安装、重启/故障恢复与真实双实例复制验收。已有 API 测试使用模拟服务命令；真实安装验收使用 process 模式。
- 复制的一致基线自动生成/传输/还原、XtraBackup 物理备份和物理恢复；当前复制配置依赖人工准备基线。
- PostgreSQL 物理备份/恢复、Doris 恢复、Oracle RMAN 灾难恢复演练；已有逻辑/快照/RMAN 备份控制链路不等于恢复验收。
- 真实 Oracle、PostgreSQL、Doris 集群的兼容性、权限、性能、故障场景验收；需专用实例、对应客户端工具和可操作的测试数据。
- 对象存储/远程备份传输与生命周期管理；当前 MySQL 文件位于 Agent、本平台快照位于 Server。
- 归档作业的真实数据一致性证明、负载保护与持久游标续跑；现有测试证明预检/执行/暂停/恢复/停止控制，不证明生产大表归档正确性。
- 更完整数据库性能指标、告警持续时间规则、抑制/维护窗口，以及备份 cron/时区日历；当前为基础指标、事件通知与固定间隔。
- 平台包含软件包/配置/密钥的完整灾备流程；当前工具只恢复两个 SQLite 库，需按运维说明另行保管其余内容。

本次没有连接或变更生产数据库，没有发布正式镜像，也没有发送真实通知。上述需真实环境的项目不能用模拟结果代替。
